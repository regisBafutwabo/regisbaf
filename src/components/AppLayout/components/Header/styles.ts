import { styled } from '@chakra-ui/react';

export const Nav = styled('nav', {
  baseStyle: {
    width: '100%',
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
});
